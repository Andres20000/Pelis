# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Pelis' do
  use_frameworks!

  # Pods for Pelis
   pod 'Alamofire', '~> 4.7'
   pod 'SDWebImage', '~> 5.0'
   pod 'SDWebImage', '~> 5.0'
   pod 'Disk', '~> 0.6.3'

  

  target 'PelisTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
